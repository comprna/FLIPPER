#ifndef _flipper_adapters_h
#define _flipper_adapters_h

#include <string>

int adapter_pos_start(std::string seq, std::string adapter);
int adapter_pos_end(std::string seq, std::string adapter);

#endif